// Inline-режим поиска картинок (см. handlers/inlineQuery.ts)

// Минимальная длина запроса, при которой ищем по эмбеддингу.
// Короче — отдаём browse-выдачу (случайные картинки).
export const INLINE_MIN_QUERY_LEN = 2;

// Сколько кандидатов берём из БД по косинусной близости. Из них тасуем и
// показываем INLINE_SHOWN_COUNT — так одна и та же фраза каждый раз даёт
// свежую выборку, оставаясь в пределах релевантного пула.
export const INLINE_POOL_SIZE = 50;

// Сколько результатов реально показываем (Telegram отдаёт до 50 за раз).
export const INLINE_SHOWN_COUNT = 25;

// Сколько случайных картинок показываем на пустой запрос (browse-режим).
export const INLINE_BROWSE_COUNT = 25;

// cache_time для answerInlineQuery (сек). Низкий — чтобы перетасовка пула была
// видна при повторном запросе; вектор закэширован, так что поиск дешёвый.
export const INLINE_CACHE_TIME = 5;
